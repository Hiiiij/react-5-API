import React from "react";
import "./App.css";
const Button = ({ selectChar }) => {
  return (
    <div>
      <button
       onClick={selectChar}>
        HAHA
      </button>
    </div>
  );
};
export default Button;
